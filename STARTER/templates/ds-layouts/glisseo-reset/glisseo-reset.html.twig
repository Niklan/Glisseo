{#
/**
 * @file
 * Glisseo reset template.
 *
 * Available variables:
 * - content
 */
#}
<{{ outer_wrapper }} class="{{ attributes.class }}" {{ attributes }}>
    {{ content }}
</{{ outer_wrapper }}>