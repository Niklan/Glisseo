{#
/**
 * @file
 * Glisseo reset template.
 *
 * Available variables:
 * - content
 */
#}
<{{ outer_wrapper }} {{ attributes.setAttribute('class', new_classes) }} >
{% if title_suffix.contextual_links is not null %}
    {{ title_suffix.contextual_links }}
{% endif %}
  {{ content }}
</{{ outer_wrapper }}>
