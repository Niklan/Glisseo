{% extends 'region.html.twig' %}

{% block region %}
  <footer{{ attributes.addClass(classes) }}>
    <div class="{{ bem_block }}__content">
      {{ block('content') }}
    </div>
  </footer>
{% endblock %}
