{% extends 'region.html.twig' %}

{% block region %}
  <footer{{ attributes.addClass(classes) }}>
    <div class="{{ bem_block }}__container">
      {{ block('content') }}
    </div>
  </footer>
{% endblock %}
