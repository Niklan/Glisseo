{% extends 'region.html.twig' %}

{% block region %}
  <footer{{ attributes.addClass(classes) }}>
    <div class="{{ bem('content', [], block) }}">
      {{ block('content') }}
    </div>
  </footer>
{% endblock %}
