{% extends 'region.html.twig' %}

{% block region %}
  <header{{ attributes.addClass(bem_classes) }}>
    <div class="{{ bem_block }}__content">
      {{ block('content') }}
    </div>
  </header>
{% endblock %}
