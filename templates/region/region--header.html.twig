{% extends 'region.html.twig' %}

{% block region %}
  <header{{ attributes.addClass(classes) }}>
    <div class="{{ bem('content', [], block) }}">
      {{ block('content') }}
    </div>
  </header>
{% endblock %}
