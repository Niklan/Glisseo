{% extends 'region.html.twig' %}

{% block region %}
  <header{{ attributes.addClass(classes) }}>
    <div class="{{ bem_block }}__container">
      {{ block('content') }}
    </div>
  </header>
{% endblock %}
