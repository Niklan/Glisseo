{% extends 'region.html.twig' %}

{% block region %}
  <header{{ attributes.addClass(classes) }}>
    <div class="{{ bem(block, 'content') }}">
      {{ block('content') }}
    </div>
  </header>
{% endblock %}
