{% extends 'region.html.twig' %}

{% block region %}
  <section{{ attributes.addClass(bem(bem_block, bem_modifiers)) }}>
    <div class="{{ bem('content', [], bem_block) }}">
      {{ block('content') }}
    </div>
  </section>
{% endblock %}
