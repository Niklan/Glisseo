{% extends 'region.html.twig' %}

{% block region %}
  <section{{ attributes.addClass(classes) }}>
    <div class="{{ bem('content', [], block) }}">
      {{ block('content') }}
    </div>
  </section>
{% endblock %}
