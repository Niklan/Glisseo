{% extends 'region.html.twig' %}

{% block region %}
  <section{{ attributes.addClass(classes) }}>
    <div class="{{ bem(block, 'content') }}">
      {{ block('content') }}
    </div>
  </section>
{% endblock %}
