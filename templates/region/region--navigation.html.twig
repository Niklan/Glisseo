{% extends 'region.html.twig' %}

{% block region %}
  <section{{ attributes.addClass(bem_classes) }}>
    <div class="{{ bem_block }}__content">
      {{ block('content') }}
    </div>
  </section>
{% endblock %}
