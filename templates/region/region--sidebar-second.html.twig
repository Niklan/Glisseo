{#
/**
 * @file
 * Theme override to display a region.
 *
 * @see template_preprocess_region()
 */
#}
{% set attributes = attributes.setAttribute('id', 'sidebar-second') %}
{% set attributes = attributes.addClass('region--sidebar-second') %}
{% if content %}
  <aside{{ attributes }}>
    {{ content }}
  </aside>
{% endif %}