{% set block = 'layout' %}
{%
  set modifiers = [
    layout.id,
  ]
%}
{%
  set classes = [
    bem(block, modifiers),
  ]
%}
{%
  set region_classes = [
    bem('region', ['content'], block),
  ]
%}
{% if content %}
  <div{{ attributes.addClass(classes) }}>
    <div{{ region_attributes.content.addClass(region_classes) }}>
      {{ content.content }}
    </div>
  </div>
{% endif %}
